import { DisplayingTimePipe } from './displaying-time.pipe';

describe('DisplayingTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayingTimePipe();
    expect(pipe).toBeTruthy();
  });
});
